package erekir;

import arc.struct.*;
import erekir.room.*;

public class ErkVars{
   /** Used for room registry. */
   public static Seq<BaseRoom> rooms = new Seq<>();
   /** Generating flag. */
   public static boolean generating;
}